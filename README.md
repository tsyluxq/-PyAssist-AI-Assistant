# -PyAssist-AI-Assistant
# 🤖 PyAssist AI Assistant  PyAssist is a simple Python command-line assistant. 🐍💻 It responds to commands like `hello`, explains what Python is, performs a basic calculation, and can be closed with `exit`. 🚀 This project demonstrates loops, functions, conditions, user input, and string manipulation. ✨
